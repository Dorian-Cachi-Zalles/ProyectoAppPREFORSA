import 'package:flutter/material.dart';

class ScreenCtrlPesos extends StatelessWidget {
  const ScreenCtrlPesos({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hello World'),
      ),
    );
  }
}
